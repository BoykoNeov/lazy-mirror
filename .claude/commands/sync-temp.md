---
description: Copy src/proxy_addon.py and src/dashboard.py to M:\temp\lazy mirro claude 3\src\ (working copy before commit)
---

Copy both source files to the working folder:

```
Copy-Item "M:\claud_projects\Lazy mirror\src\proxy_addon.py" "M:\temp\lazy mirro claude 3\src\proxy_addon.py" -Force
Copy-Item "M:\claud_projects\Lazy mirror\src\dashboard.py"   "M:\temp\lazy mirro claude 3\src\dashboard.py"   -Force
```

Run both via the PowerShell tool, confirm both copies succeeded.
